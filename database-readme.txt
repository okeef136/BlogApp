Easiest way to clean/create the databases:

(1) If blog databases already exist, run drop-databases.sql first.
(2) Run create-databases.sql
(3) Run prod-database.sql in the SQL section of the 'blog' database
(4) Run test-database.sql in the SQL section of the 'blog_test' database
